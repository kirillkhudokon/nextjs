export type Image = {
  url: string
  variants: {
    base: string
    large: string
    medium: string
    small: string
  }
  id: number
  name: string
  createdAt: string
  updatedAt: string
  PostId: number
}